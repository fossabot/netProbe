#
# initialize a raspberry pi from a raspbian lite image
#
# Time-stamp: <2016-07-02 11:47:24 alex>
#
#
# usage :
#  ansible-playbook -i files/hosts playbooks/pi-init.yml --limit %IP%
#   --ask-pass: first use
#   --tags "upgrade": for OS upgrade

---
- name: PI INIT
  hosts: piprobes
  tags: init
  become: true
  become_user: root
  remote_user: pi

  tasks:
   - include : pi-init-init.yml

- name: PI UPGRADE
  hosts: piprobes
  tags: upgrade
  become: true
  become_user: root
  remote_user: pi

  tasks:
    - include : pi-init-upgrade.yml
